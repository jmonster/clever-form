{{input readonly=readonly
        required=required
        autofocus=autofocus
        value=value
        placeholder=placeholder
        disabled=disabled
        size=size
        tabindex=tabindex
        maxlength=maxlength
        name=name
        min=min
        max=max
        pattern=pattern
        accept=accept
        autocomplete=autocomplete
        autosave=autosave
        formaction=formaction
        formenctype=formenctype
        height=height
        inputmode=inputmode
        multiple=multiple
        step=step
        width=width
        form=form
        selectionDirection=selectionDirection
        spellcheck=spellcheck
        maxlength=maxlength
        style=style
        type=type
        autocorrect=autocorrect
        autocapitalize=autocapitalize
        spellcheck=spellcheck
}}
<label for=name class="clever-input-label animated {{classWhenActive}}">{{label}}</label>
